# uir Package Manager

A lightweight universal package manager written in Go.

## 📖 About

uir (Universal Package Manager) is a simple yet powerful package management system designed for easy software distribution and management. It provides clean CLI interface, self-updating capability, and reliable package handling.

## 🚀 Features

- **Simple Package Management**: Install, remove, and manage packages with ease
- **Self-Contained**: uir can update or remove itself as a package
- **Binary Link Management**: Automatic symlink creation for executables
- **Dependency Tracking**: Basic dependency support for packages
- **Clean CLI**: Intuitive command-line interface
- **Self-Updating**: Built-in update mechanism for the package manager itself

## 🛠️ Installation

### Quick Install
```bash
git clone https://github.com/Semyon5700/uir.git
cd uir
chmod +x install.sh
sudo ./install.sh
